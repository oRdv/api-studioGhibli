'use strict';

const url = 'https://ghibliapi.vercel.app';

let id

const filmes = 'https://ghibliapi.vercel.app/films/'

const criarSections = (filmes) => {
    
    const text = document.createElement('div')
    text.classList.add('text')

    const titleGs = document.createElement('div')
    titleGs.classList.add('titleGs')
}